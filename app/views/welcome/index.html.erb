<!DOCTYPE html>
<html>
<head>
  <title>get JSON dat</title>
  <script type="text/javascript" src="http://maps.googleapis.com/maps/api/js?sensor=false"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>


  <script>
    var map;
    $(document).ready(function(){
      $("button").click(function(){
        $.getJSON("https://serene-brushlands-22102.herokuapp.com/welcome/parsefile", function(arr){

          var out = "<table><tr><th>latitude</th><th>longitude</th></tr>";
          var i;

          for (i = 0; i < arr.length; i++) {
            out += "<tr><td>" +
                arr[i].latitude +
                "</td><td>" +
                arr[i].longitude +
                "</td><td>";

          }
          out += "</table>"


          $("button").html(out)


          var mapOptions = {
            center: { lat: 32.730468, lng: -97.114774},
            zoom: 3
          };
          map = new google.maps.Map(document.getElementById('map-canvas'),mapOptions);

          for (var i = 0, length = myArr.length; i < length; i++) {
            var data = myArr[i],
                latLng = new google.maps.LatLng(data.latitude, data.longitude);

            // Creating a marker and putting it on the map
            var marker = new google.maps.Marker({
              position: latLng,
              map: map,
              title: data.baloon_text
            });
          }


          });
      });
    });



  </script>
</head>
<body>

<button>Get JSON data</button>

<div id="map" style="width: 550px; height: 450px"></div>

</body>
</html>
